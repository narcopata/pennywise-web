export const QUERY_KEYS = Object.freeze({
	COMPANIES: Object.freeze({
		MINE: Object.freeze(["companies", "mine"]),
		MINE_INCOME: Object.freeze(["companies", "mine", "income"]),
		MINE_EXPENSE: Object.freeze(["companies", "mine", "expense"]),
	}),
});
